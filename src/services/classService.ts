
// Re-export from the new modular class services for backwards compatibility
export { 
  fetchScheduledClasses,
  createScheduledClass,
  updateScheduledClass,
  deleteScheduledClass,
  type ScheduledClass,
  type Profile
} from './class';
